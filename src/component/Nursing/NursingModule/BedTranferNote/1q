/* Adan  15-11-24 */
.BedTransferNote-container{
    font-family: Arial, sans-serif;
    background-color: var(--background-color);
    padding: 4px;
    /* font-size: 12px; */
    /* height: 650px; */
    /* display: flex; */
  }
  .BedTransferNote-header{
      background-color: var(--upperBar-bg-color);
      color: white;
      padding: 8px;
      margin-bottom: 4px;
      font-size: var(--heading-font);
  }
  .BedTransferNote-content{
      display: flex;
      gap: 4px;
  }
  .BedTransferNote-Section{
      padding: 8px;
      border: 1px solid #ccc;
      background-color: white;
  }
  .BedTransferNote-Section-header{
      background-color: var(--upperBar-bg-color);
    color: white;
    padding: 4px 8px;
    font-size: var(--heading-font);
    margin: 5px 0px;
  }
  .BedTransferNote-Form{
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 6px;
      gap: 4px;
      font-size: var(--font-size);
  }
  .BedTransferNote-Form label{
      min-width: 245px;
  }
  .BedTransferNote-Form input,.BedTransferNote-Form textarea{
      padding: 5px;
      border: 1px solid gray;
      border-radius: 5px;
      width: 120%;
  }
  .BedTransferNote-Form select{
      padding: 5px;
      border: 1px solid gray;
      border-radius: 5px;
      width: 130%;
  }
  
     /* Button Section */
    .BedTransferNote-navbar-btns {
      /* position: fixed;
      top: 60px;
      right: 0;
      width: 140px;
      background-color: #f0f0f0;
      border-left: 1px solid #ccc;
      height: calc(100% - 60px);
      padding: 10px;
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 10px;
      overflow-y: auto;  */
      position: fixed;
      padding: 10px;
      bottom: 10px;
      right: 10;
      display: flex;
      align-items: center;
      gap: 10px;
      overflow-y: auto;
      /* top: 60px; */
      /* left: 0; */
      /* width: 140px; */
      /* background-color: #f0f0f0; */
      /* border-left: 1px solid #ccc; */
      /* height: calc(100% - 60px); */
      /* flex-direction: column; */
    }
    
      .BedTransferNote-navbar-btns button {
          /* width: 100%; */
          /* padding: 10px;
          font-size: var(--font-size);
          background-color: var(--button-bg-color);
          color: white;
          border: none;
          border-radius: 4px;
          cursor: pointer;
          text-align: center; */
          background-color: var(--button-bg-color);
          color: var(--button-hover-background-color);
          font-size: var(--font-size);
          padding: 5px;
          border: 1px solid var(--button-bg-color);
          border-radius: 5px;
      
        }
        
        .BedTransferNote-navbar-btns button:hover {
          background-color: var(--button-hover-background-color);
          color: var(--button-bg-color); 
        }
    
  
